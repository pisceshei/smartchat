"""Flow engine package (plan 附錄 B.1). graph_schema defines the canvas model,
port conventions, validation and trigger extraction; runtime execution is added
by the flow-engine subsystem."""
from __future__ import annotations
