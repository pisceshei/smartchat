"""分流連結 (split links): base62 slug, random/time_period/sequential routing
over multi-account targets, server-side QR (segno), click stats + the
attribution loop shared with the edge redirect (plan B.3)."""
from __future__ import annotations
