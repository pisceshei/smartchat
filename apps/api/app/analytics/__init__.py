"""Reports/analytics engine (plan 附錄 B.4): event → hourly aggregate rollups,
nightly distinct-count day tables, agent online-time tracking, CSV export and
the nightly AI digest. The ``events`` table is the single raw store."""
