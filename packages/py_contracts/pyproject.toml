[project]
name = "py-contracts"
version = "0.1.0"
description = "SmartChat shared contracts: events, content blocks, LLM client, markers"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.8", "httpx>=0.27", "uuid7>=0.1"]

[tool.setuptools.packages.find]
where = ["."]
include = ["py_contracts*"]
